{
  "id": "delta",
  "version": "1.0.0",
  "name": "Delta (via Github Releases)",
  "description": "Delta is a general-purpose command-line fuzzy finder.",
  "options": {
    "version": {
      "default": "latest",
      "description": "Select the version to install.",
      "proposals": [
        "latest"
      ],
      "type": "string"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers-extra/features/gh-release"
  ]
}
